# Renomme ce fichier en `.env` pour le développement local (vercel dev).
# En prod, configure ces variables dans le dashboard Vercel
# (Settings → Environment Variables) — JAMAIS dans le repo.

# Mode test pour le développement
STRIPE_SECRET_KEY=sk_test_REPLACE_ME

# Mode live pour la production (à mettre uniquement sur l'environnement Production de Vercel)
# STRIPE_SECRET_KEY=sk_live_REPLACE_ME
